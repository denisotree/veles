# Canonical English strings. All other locales are translations of these
# keys; missing keys in a non-EN locale fall back here at lookup time.

[project_wizard]
intro_no_project = "No Veles project found at {cwd}."
ask_initialize = "Initialize this directory as a Veles project? It will add `.veles/` and an `AGENTS.md` template, and index existing project files."
created_state = "  · created {state_dir} and AGENTS.md skeleton."
ready = "<project '{name}' ready. edit AGENTS.md to add context.>"
ask_provider_override = "Pin a project-specific provider/model? (default: inherit user-level)"
ask_provider_label = "Provider"
ask_model_label = "Model id (blank to skip)"
provider_written = "  · wrote provider override → {path}"
ask_wiki_seed = "Seed wiki/sources/ from existing docs ({sample}{extra})?"
wiki_seed_done = "  · copied {count} file(s) into wiki/sources/ (run `veles add` later to ingest)"
ask_channel = "Connect a chat channel (e.g. Telegram) to this project?"
ask_channel_type = "Channel type"
channel_field_required = "  ! a required {channel} field was left blank; skipping channel setup."
channel_written = "  · wrote {channel} channel → {path}"
indexed_wiki = "  · indexed {pages} wiki page(s) into the FTS database."

[rules_digest]
header = "House rules (learned from this project)"
group_preference = "Preferences:"
group_dont = "Don't:"
group_do = "Do:"
group_format = "Output format:"

[goal]
confirm_line = "Let me check I got this right: {summary}"
confirm_actions = "Confirm (yes / continue / go ahead), describe edits, or cancel (cancel)."

[telegram]
start_greeting = "Hi! I'm Veles. Just send a message and I'll reply. Tip: /help lists the commands."
history_cleared = "History cleared."
history_empty = "History is already empty."
