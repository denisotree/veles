"""Built-in `organize` module (M175) — layout-driven project tidy-up.

This is a built-in Python module (peer to `modules/wiki/`), NOT core: the
operation dispatcher and the `move_file` primitive live here so core stays
"memory + learning loop + orchestrator + provider protocol + minimal
tools/skills" (VISION §5.2). The `veles organize` command
(`cli/commands/organize.py`) builds and runs the agent on top of them.
"""
